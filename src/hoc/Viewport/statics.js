export const win = typeof window != 'undefined' && window
export const doc = typeof document != 'undefined' && document
export const docEl = doc && doc.documentElement
export const winScreen = win && win.screen
